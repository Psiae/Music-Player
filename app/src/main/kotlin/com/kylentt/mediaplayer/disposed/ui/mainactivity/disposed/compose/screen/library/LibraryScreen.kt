package com.kylentt.mediaplayer.disposed.ui.mainactivity.disposed.compose.screen.library

import androidx.compose.runtime.Composable

@Composable
fun LibraryScreen() {
  /*Column(
      modifier = Modifier
          .fillMaxSize()
          .verticalScroll(rememberScrollState()),
      horizontalAlignment = Alignment.CenterHorizontally,
      verticalArrangement = Arrangement.Center
  ) {
      StatusBarSpacer()
      Text(text = "Library Screen", color = DefaultColor.getDNTextColor())
      Spacer(modifier = Modifier.height(1000.dp))
      Text(text = "End Of Column", color = DefaultColor.getDNTextColor())
  }*/
}
