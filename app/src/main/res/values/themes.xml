<?xml version="1.0" encoding="utf-8"?>

<!-- Will try to empty xml's just move them to .kt somehow -->

<resources>

    <style name="Theme.MediaPlayer" parent="android:Theme.Material.Light.NoActionBar">

    </style>

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_screen_background</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_screen_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/blu_splash</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.MediaPlayer</item>
    </style>
</resources>