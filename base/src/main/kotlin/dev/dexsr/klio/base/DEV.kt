package dev.dexsr.klio.base

object DEV {
    // Time in milliseconds updated on each commit, used to validate time, technically any `now`
    // epoch timestamp should be more than this
    const val LAST_STAGING_MILLIS_FROM_UNIX_EPOCH_UTC = 1699040535000
}
